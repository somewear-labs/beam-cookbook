// Package stream provides named, full-duplex byte streams over Grid datagram
// delivery. It owns framing, flow control, lifecycle, and built-in service wire
// formats while leaving transport delivery and user interaction to callers.
package stream

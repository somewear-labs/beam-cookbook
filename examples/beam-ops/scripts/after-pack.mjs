// afterPack hook — placeholder. rpc binary is no longer bundled in the app;
// it is installed separately to ~/bin/rpc via install-rpc.sh or make ship-local.
export default async function afterPack() {}
